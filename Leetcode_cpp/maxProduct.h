#ifndef MAXPRODUCT_H_INCLUDED
#define MAXPRODUCT_H_INCLUDED
#include <vector>
#include <algorithm>
using namespace std;
class Solution{
public:
    /**
        find the contiguous subarray within an array
        [2,3,-2,4]
            return 6=[2,3]
    */
    int maxProduct(vector<int> &nums){
        int length = nums.size();
        vector<int> f(length);

    }
};


#endif // MAXPRODUCT_H_INCLUDED
